# RidgeSeeker (formerly EdgeFinder): AI Handoff / Technical Spec

**Purpose of this file:** you are an AI assistant helping the owner (a hobbyist sports bettor, not a professional developer) modify this program. This document tells you what the program is, how it is built, and which decisions are deliberate so you do not undo them while helping. Read it fully before proposing changes.

**Doc version:** 2026-07-06 (v14.3). Update the changelog at the bottom whenever you change the code.

---

## 0. THE ONE RULE THAT PREVENTS CONFUSION

Every time you change the code, hand the owner back BOTH the updated `ridgeseeker.py` AND an updated `HANDOFF.md` with a new changelog entry, and tell them to commit both together. This file is only accurate if it is kept in sync. The single most common failure is code changing while this doc goes stale, which leaves the next AI confused. Do not let that happen.

---

## 0b. File handback protocol (the owner uploads via the GitHub web UI)

The owner updates the repo by unzipping what you send and using GitHub's Add file -> Upload files at the repo ROOT. That upload overwrites same-named root files but CANNOT place files into `.github/workflows/`. Therefore: hand back only root-level files (`ridgeseeker.py`, `HANDOFF.md`, `FUTURE.md`, `README.md`, `AUDIT_TODO.md`, `AUDIT_ORIGINAL.md`, `AUDIT_DELIVERABLES.md`, `.gitignore`). If the workflow must change, give the owner the full file contents to paste via the web editor at `.github/workflows/ridgeseeker.yml`, and say so explicitly. Never hand back `ridgeseeker_betlog.json`, `ridgeseeker_snapshots.json`, `ridgeseeker_runlog.json`, or `ridgeseeker_probables.json`: the bot owns those.

---

## 1. Prime directives (read first)

1. **Do not rebuild from scratch.** This code is the product of many careful iterations against live data. Prefer surgical edits. If you believe a rewrite is warranted, say so explicitly and explain the tradeoff before doing it.
1b. **Features are in scope (owner directive 2026-07-03).** The audit's original "do not add features" line is amended out (see AUDIT_ORIGINAL.md header). Scout and propose accuracy/profitability features every session; maintain the FUTURE.md roadmap; implement Tier-NOW items in code batches. Decision-affecting features ship log-first and activate only at their pre-registered N-gates, with a MODEL_VERSION bump.
2. **Honesty over polish is the core philosophy.** The point of this tool is to surface real edges and output "no edge / pass" when there is not one. Never add logic that manufactures signals, inflates grades, or fabricates data to look busier. "No plays today" is a correct, valuable output.
3. **Never use em dashes** anywhere: not in code, comments, UI text, or your chat replies to the owner. The owner forwards output to other people and em dashes read as AI-authored. Use commas, colons, or parentheses. There are currently zero em dashes in the file; keep it that way. Empty table cells use a plain hyphen "-".
4. **Real data only.** Every number shown must trace to an actual API response. If a source is unavailable, show "no data" honestly rather than estimating and presenting it as fact.
5. **Serve the underlying goal, not just the literal request.** If a smarter framing or a split solution better serves what the owner actually cares about, propose it. Ask clarifying questions when they improve the result. The owner explicitly values this.

---

## 2. What the program does

RidgeSeeker is a single Python script (`ridgeseeker.py`, ~1550 lines) that:
1. Fetches MLB odds (The Odds API) and sharp-money splits + live status + final scores (Action Network, free/no-auth).
2. Computes a "fair" probability anchored to DEVIGGED PINNACLE (fetched via the eu region of The Odds API; every play carries `anchor`: 'pinnacle' or 'consensus'). Devig uses the POWER method (`fair_pair`), not proportional scaling: proportional devig overstates the fair probability of the longer-priced side and inflated stated EV by ~1.3 to ~4.3 points on the +150 to +250 dogs this tool prefers. The old proportional number is still logged on every play as `fair_mult` for later comparison. When Pinnacle skips a market or line, it falls back to the no-vig median of ~25 books (also power-devigged). Compares fair to Bovada and flags **value** (EV >= 3%).
3. Grades **sharp money** S/A/B/C/D from the money%-minus-tickets% gap, with contrarian and steam confirmation.
4. Suggests a **unit size** (1u / 1.5u / 2u) per play, with a hard longshot cap.
5. Tracks results automatically: logs plays with the full capture-or-lose feature set (`MODEL_VERSION`, `ev`, `fair`, `anchor`, raw Pinnacle prices, raw ticket/money splits, `run_ts`), refreshes each pending play's close (price AND devigged fair prob) every pregame run, grades against final scores, computes units/ROI plus TWO closing-line metrics (`clv` = price vs Bovada close, `clv_fair` = EV at the devigged close), and reports level-up progress plus open exposure.
5b. Runs a zero-unit SHADOW ledger beside the real one (v14.3 "Signal lab"): every graded sharp lean S through D (sharp-side ML) and every value flag is logged, closed, and graded exactly like a real play but with 0 units, in its own key namespace, excluded from the record, gates, and the pre-registered endpoint. It exists to answer "do C leans or non-agreeing value flags carry EV" with data instead of opinion.
6. Snapshots every graded game each run for edge-over-time analysis.
7. Renders one mobile-first HTML dashboard with a **Board / Results pill toggle** (both views live in the same file, no page reload), plus CSV exports.

Runs on **GitHub Actions** (cloud): 2 full runs/day plus an optional cheap close-capture run (`RS_MODE=close`, h2h only, 2 credits) shortly before the evening slate. Publishes to **GitHub Pages**. Also runs locally on Windows (opens a browser). One file, auto-detecting via the `CI` flag.

---

## 3. File structure (single file: ridgeseeker.py)

Top to bottom:

| Section | What it does |
|---|---|
| CONFIG | `ODDS_KEY` (env or key file), `RUN_MODE` (`RS_MODE` env: full/observe/close; observe = full pipeline, never logs plays, for the opener board), `VENUE_GEO` + `PARK_RF_APPROX` static tables, `BOOKMAKERS_PARAM` (F26, default OFF; enable via `RS_BOOKMAKERS=1` only after the owner curl verifies MLB coverage and h2h_lay billing; auto-appends +bk10 to MODEL_VERSION), `SPORTS` (multi-sport registry: MLB/NFL/NBA/NHL/CFB/CBB enabled with season-month windows, WNBA present but disabled; off-season sports are never fetched; `RS_SPORTS` env overrides for testing), `PLAN_CREDITS` (credit-budget warning threshold), `GRADE_THRESHOLDS`, sanity gate, `UNIT_DOLLARS`, `MAX_DAILY_UNITS` |
| SSL fetch (`_ssl_tiers`, `gj`) | Layered SSL fallback. Do not simplify (see section 6). |
| Odds math | `am2prob`, `am2dec`, `prob2am`, `novig`, `novig3`, `gate` |
| Data fetch | `fetch_odds`, `fetch_sharp_and_status` (returns `(sharp, status, raw)`; `raw` reused for final scores), `soft_fair_map` |
| Engine (`analyze_game`) | Builds ML/spread/total plays, no-vig fair, EV, value gate, consensus-favorite guard |
| Grading (`grade_sharp`) | Sport-aware S/A/B/C/D (see section 4) |
| Sizing (`suggest_units`) | 1u/1.5u/2u with longshot cap (see section 5) |
| Recommendation (`build_recommendation`) | Picks which market to name; hybrid value-vs-sharp |
| Tracker | `load_log`, `log_plays`, `build_shadow_plays`, `log_shadow_plays`, `grade_pending`, `_grade_one`, `tracker_summary`, `collect_results` |
| Snapshots | `hours_until`, `log_snapshots` |
| Stats | `compute_stats` (aggregations by grade/units/market/signal/price/time-to-game) |
| Templates | `TEMPLATE_HEAD` (HTML shell + all CSS), `TEMPLATE_APP` (all JS, including the Board/Results toggle and `renderResults`) |
| `main()` | Orchestrates, writes outputs |

The frontend is two raw-string templates. Python injects `const ALL = {...}` (card data + `_top`, `_tracker`, `_stats`, `_unit_dollars`) between them. The JS reads `ALL` and builds the DOM. **There is exactly ONE stats system: the Results tab, rendered client-side by `renderResults()` from `ALL['_stats']`.** (An older standalone `build_stats_page` function was removed in v8; if you see references to `stats.html` anywhere, they are stale.)

---

## 4. Grading (DELIBERATE, do not loosen)

Sharp grade is driven by **gap = money% minus tickets%** on the sharp side. Thresholds are **percentile-anchored to the real MLB gap distribution** (median ~9, 75th ~15, 90th ~22), in `GRADE_THRESHOLDS`:

```
baseball: S:25, A:20, B:13, C:9, D:6
```

`grade_sharp` logic:
- **S** = gap >= 25 AND contrarian AND steam (all three).
- **A** = (gap >= 20 AND contrarian) OR gap >= 25.
- **B** = gap >= 13. **C** = gap >= 9. **D** = gap >= 6. Below 6 = None.
- **contrarian** = sharp side has <= 35% of tickets.
- **steam** = sharp side's Action-Network price implies prob > soft-book fair by > 1.5 pts. This is a snapshot proxy, NOT true reverse-line-movement (no opening-line history available). Label it "steam", never "RLM".
- **Non-contrarian penalty:** if not contrarian and tickets >= 55, force D.
- **Thin cap:** if num_bets < 1500, cap at B.

**Why:** an earlier version graded on raw gap and produced far too many A/S (a +15 gap graded A, a 75th-percentile event, way too common). The owner wants B to be the average and A/S rare "chase it" bets. If you are making A/S more common, you are going the wrong way. Other sports get their own thresholds once calibrated; they currently fall back to `_default` (= baseball).

---

## 5. Unit sizing (DELIBERATE, the longshot cap is the guardrail)

`suggest_units` returns (units, reason). The owner bets underdogs; the principle is **a long price is already its own reward, so do not double down on variance.**

- **2u** (rare): has_value AND EV >= 8% AND sharp A/S agrees with the value side AND price <= +200.
- **1.5u**: (has_value AND 4% <= EV < 8%, not longshot) OR (pure S with contrarian+steam, no value).
- **1u**: any single real signal, OR anything at +250 or longer regardless of grade.
- **skip** (None): C/D with no value.

**The +250 longshot cap is non-negotiable** unless the owner explicitly changes it.

Level-up gates (`LEVELS`): $10->$20 needs ~50 settled bets AND >= +5u AND ~$800 bankroll; $20->$50 needs ~175 bets AND >= +12u AND ~$2000. A "profit concentrated in one lucky hit" flag blocks leveling up on a fluke. These sample sizes are deliberate; do not shrink them.

**Exposure guidance (v11):** `MAX_DAILY_UNITS` (6u) is guidance, not a filter. Plays past it are still LOGGED (the paper-trade tracker wants all data); the tracker bar and console flag the total so real-money days get prioritized by EV. One pending play per game is enforced in `log_plays` (the rec can flip markets between runs; without the guard that logs correlated double exposure on one game). Kelly context, documented in AUDIT_TODO F21: at the tool's typical stated edges (4% at ~+170) full Kelly is ~2.4% of bankroll; 1u = $10 on the $800 level-up floor is ~1.25%, about half Kelly IF stated edges are real, which is the right conservatism while they are unproven. Do not add Kelly sizing before calibration is proven (FUTURE.md gate).

---

## 6. SSL fetch (DELIBERATE, do not clean up)

`_ssl_tiers` + `gj` try, in order: normal verified context, certifi + widened ciphers (SECLEVEL=1), system store + widened ciphers, last-resort no-verify. It caches whichever works. Every tier fixed a real failure (corporate TLS inspection, cert-store issues, handshake failures). On GitHub runners tier 0 works instantly; the rest are for local/managed machines. **Do not reduce this to a plain `urlopen`.** The owner's work laptop firewall blocks the odds APIs entirely, which is WHY this runs on GitHub (fetch happens on GitHub's network). Keep it cloud-runnable.

---

## 7. Data sources and quirks

- **The Odds API** (`fetch_odds`): key via `ODDS_KEY` env (GitHub secret) or gitignored `odds_key.txt` for local; never hardcoded (see the v10 note below). **Cost = markets x regions per call.** Full run: `regions=us,eu` (2) x `h2h,spreads,totals` (3) = **6 credits**. Close run: 2 regions x h2h = **2 credits**. Free tier ~500/month; the current schedule uses 14/day = ~420/month. Adding a sport, region, or run frequency can blow the budget. Do the math before changing cadence.
- **Action Network** (`fetch_sharp_and_status`): free, no auth, needs a browser User-Agent. Provides ticket%/money% splits, live status with real inning, and final scores (`boxscore.stats.{away,home}.runs`). US team sports only. Source for grades, live badges, AND result grading. If down, value scanning still works but grades/results do not.
- **Score backfill** (`fetch_scores_for_dates`, v10): every run also pulls the AN scoreboard for the past 3 days via `?date=YYYYMMDD` (free). Without this, night games finishing after the day's last run NEVER got graded (a real logged play sat pending 2 days). Results are a LIST of finals per matchup with start times; `_pick_result` matches a play to the correct game via its logged `commence` time (or log date for legacy plays) and honestly skips true ambiguity. Do not remove the backfill.
- **API key (v10):** `ODDS_KEY` env var, or `odds_key.txt` next to the script for local runs (gitignored). NEVER hardcode a key: the repo is public. The pre-v10 key was exposed in git history and must stay rotated out.
- **Pinnacle: the fair-value anchor (v9).** The old claim that Pinnacle is paywalled on free feeds was WRONG: The Odds API serves Pinnacle in the `eu` region on the free tier. `fetch_odds` now requests `regions=us,eu`, which DOUBLES the credit cost: 3 markets x 2 regions = **6 credits/run**. At 2 full runs/day plus the 2-credit close-capture run that is 14/day, ~420/month of the ~500 free, leaving headroom for ~13 manual full runs. Do not add sports, regions, or scheduled runs without redoing this math.
- **CLV (closing line value), v9, reworked v11:** every play is SEEDED with close = entry at log time (before v11, a play only got a close if a later run happened while its game was still pregame, which silently excluded all day games and everything logged at the 21:30 run: the primary metric covered a biased minority of plays). `update_closes` then refreshes `close_price` (Bovada) AND `close_fair` (devigged fair prob, Pinnacle-anchored when available) on every pregame observation, bumping `close_obs`. At grading, `clv` = entry vs Bovada close (same-book price movement) and `clv_fair` = stated-EV formula re-run with the closing fair prob (the market's final EV verdict; this is the headline metric and the correct answer to "should CLV be vs Pinnacle": compare in devigged probability space, never across two books' vigged prices). Only `close_obs > 0` plays count as measured; stats exclude entry-seeded closes rather than averaging them in as zeros, and report coverage. The close is still the last reading this tool saw; the UI says so honestly. Do not remove any of this, and do not present the approximate close as a true close.
- **Close-capture mode (v11):** `RS_MODE=close` fetches h2h only (2 credits), runs `update_closes` + grading, and exits: no new plays (an h2h-only board would bias play selection), no snapshots, no dashboard. The workflow fires it at 22:45 UTC because the main night block starts 23:05-23:10 UTC (7:05pm ET); a later run would miss the East slate. Spread/total closes freeze at the last full-run observation in this mode, deliberately.
- **Doubleheaders and duplicates (v10):** plays now log the Odds API `event_id` and `commence` time. Dedupe keys on event_id (prevents re-logging tomorrow's game tomorrow), close refresh matches by event_id, and result grading matches by start time. `update_closes` also requires the game start to be in the FUTURE BY CLOCK, not just status 'scheduled', so a downed status feed can never let live prices contaminate the close (CLV integrity). Pushes grade as 'push' with 0 units_pl instead of pending forever; W/L record and ROI exclude them by convention. Plays still pending 72h after their scheduled start grade as 'void' (0 units_pl, excluded like pushes, `void_reason` recorded): if 3 days of score backfill produced no matchable final, the game was postponed, cancelled, or rescheduled outside the 6h match window, and a sportsbook would refund the bet. Without this, one rainout pollutes the pending count and open-exposure total forever.

---

## 8. Outputs and persistence

Each run writes:
- `docs/index.html` (CI) or `ridgeseeker_latest.html` (local): the dashboard (contains both Board and Results tabs).
- `docs/bets.csv`, `docs/snapshots.csv`: raw data for manual sorting.
- `ridgeseeker_betlog.json`: recommended plays + graded results, PLUS zero-unit shadow rows flagged `shadow: true` (v14.3 Signal lab; every stats and tracker aggregate filters real vs shadow first). Auto-migrated from `edgefinder_betlog.json` on first run; never delete either without a backup.
- `ridgeseeker_snapshots.json`: per-run per-game readings for edge-over-time (auto-migrated). NOTE: the old workflow never committed the snapshots file, so CI snapshots were silently lost; the v9 workflow commits it.
- `ridgeseeker_history/`: timestamped HTML archives.

On GitHub the workflow commits these back so state persists across ephemeral runs and time off. Do not move persistence to anything needing a database or paid service; the commit-back pattern is deliberate and free.

---

## 9. Frontend conventions

- Mobile-first (~410px), dark theme, CSS variables at the top of `TEMPLATE_HEAD`.
- **Board / Results** views toggled by pills via `showView('board'|'results')`, matching the owner's weather tracker for consistency. Results builds lazily on first open from `ALL['_stats']` via `renderResults()`.
- Grade colors: S gold `#f5c451`, A green `#34d399`, B blue `#60a5fa`, C/D grey. Keep consistent.
- No em dashes in any string. Empty cells use "-".
- The parlay feature was removed on purpose. Dashboard tabs are now GENERATED from the Python SPORTS registry (single source of truth); do not hand-edit the JS tab list.

---

## 10. Validate before handing back

1. `python -c "import py_compile; py_compile.compile('ridgeseeker.py', doraise=True)"` passes.
2. `EDGEFINDER_CI=1 python ridgeseeker.py` runs and writes `docs/index.html` with no error.
3. The generated HTML's JS runs with no reference errors, and BOTH Board and Results views render (you can eval the two script blocks with a stubbed DOM and call `showView('results')`).
4. Zero em dashes introduced.
5. You did not loosen grades, remove the longshot cap, simplify the SSL tiers, or break commit-back persistence.
6. You updated this file's changelog and told the owner to commit both files.

---

## Changelog

- **v14.3 (2026-07-06), Results tab crash fix (AUDIT_TODO F46) + Signal lab shadow ledger (F47). MODEL_VERSION unchanged: item 1 is a display fix, item 2 is log-only rows in their own namespace; the real-play decision path is byte-identical.**
  (1) F46, THE BUG THE OWNER SAW ("results board isn't showing up"): renderResults referenced a lowercase `s` in three places (`s.ev_real`, `s.clv_roll`, `s.clv_seg` plus the segment guard) while the stats object is bound as uppercase `S`, and the `pct` helper was a block-scoped const inside the CLV card but used by the drift and segment cards outside that block. Both shipped with the v12.1 monitoring cards and stayed latent because the zero-settled-bets early return dodges that code path; the first bet that graded after v12.1 made showView('results') throw ReferenceError before host.innerHTML was ever assigned, leaving the Results pane permanently blank while the Board rendered fine (the error only shows in the browser console). Reproduced and verified fixed per section 10 item 3 with a stubbed-DOM Node harness; `pct` is now defined once at function scope. Fourth instance of the F35 bug class in spirit: code that only executes under a data condition that did not exist at ship time. Validation now covers the settled-bets path explicitly.
  (2) F47, Signal lab (the owner's question "are the C's positive EV?" was structurally unanswerable): the betlog only ever receives hero plays (S/A grades and double-downs), so C and D sharp leans, B sharp-only leans, and value flags without sharp agreement were never logged, and FUTURE.md section 1's "compare S/A vs B/C" watch item had no uniform data to run on. Every full run now also logs zero-unit SHADOW rows: (a) the sharp side's moneyline at Bovada for EVERY graded game S through D (uniform basis: same market, same book, entry frozen at first sighting like real plays), and (b) the value play on every has_value game. Shadow rows carry shadow=True and shadow_kind ('sharp'/'value'), live in their own dedupe-key namespace INCLUDING the kind (on a double-down the sharp lean and value flag are the same event/market/side and both ledgers need their row; without the kind in the key the second row silently dropped, caught in testing), allow one pending row per (event, kind), and flow through the normal close-refresh, grading, and void machinery, so each earns the same clv_fair headline metric as real plays. They are excluded from the record, tracker bar, open exposure, level gates, and the pre-registered clv_fair endpoint (compute_stats and tracker_summary filter on the shadow flag first; moving goalposts by contaminating the endpoint pool was the main design risk). The one-pending-play-per-game guard in log_plays now explicitly excludes shadows so a pending shadow can never block a real play (tested: a C shadow logged at 15:00 does not stop the same game's A-grade real play at 21:30). Results tab gains a "Signal lab (shadow board, not bets)" table (win%, flat 1u ROI, EV at close per grade plus the value flag row), rendered on BOTH the settled and the no-settled-bets paths because a C-only stretch is exactly when it matters. bets.csv +2 columns (shadow, shadow_kind), runlog +shadows count. Cost: zero API credits (everything was already fetched), roughly 5-12 betlog rows per day. No suppressions apply to shadow rows (capture-or-lose; pin_age_min is stamped so stale-anchor rows can be filtered in analysis, and clv_fair only depends on the CLOSING fair, so entry-anchor staleness cannot corrupt the shadow metric). Observe and close modes never log shadows, keeping the two ledgers on identical entry timing.

- **v14.2 (2026-07-05), live-board findings (AUDIT_TODO F45), spotted by the OWNER reading the dashboard:** (1) F45, wrong-day inheritance: sharp splits, status, and grades are joined by (away, home), and MLB series repeat the same matchup on consecutive days. The evening board lists both games, so TOMORROW's card inherited TODAY's splits, live/final status, and sharp grade; a phantom next-day sharp rec at 21:30 was possible. Every AN entry now carries its start_time and the join requires the odds commence within 12h (missing timestamps keep old behavior rather than dropping data). Wrong-day board chips are also cleared. (2) F45b: rec chips rendered on live and final games (a live -1100 line displayed as a recommendation someone could bet by hand); recs now build only for clock-pregame games. Logging was never at risk (F25's clock guard held), this closes the display path. (3) F45c, found underneath while fixing: analyze_game receives a splits-only sharp map, so the F37 an_ml capture NEVER reached snapshots or plays (always None since it shipped). Third instance of the F35 bug class: a field tested at the fetch boundary but not across the attachment boundary. an_ml now attaches at the main loop where the full entry exists, and the test crosses log_snapshots for real. No MODEL_VERSION bump: all three are input-correctness and display fixes, not model changes. ALSO CONFIRMED WORKING AS DESIGNED: the all-D July 4 board. Grades require confirmation (contrarian tickets <= 35, or steam) above raw gap; a gap-14 game with tickets at 59 is just a popular favorite, and every splits game that night had public and money on the same side. Zero plays logged was the correct output. Thresholds stay untouched per F33 (changes only at N-gates).

- **v14 (2026-07-04), research-report integration (AUDIT_TODO F43). MODEL_VERSION BUMPED to v13-research1: two suppression rules change play selection.** (1) Kalshi fee model CORRECTED against the official June 2026 schedule: taker = round-up(0.07 x contracts x P x (1-P)) per ORDER to the cent; maker fees now exist (modeled at 25% of taker, the third-party consensus where charged; special-event flat per-contract maker fees are NOT modeled, assumption A11); per-order cent rounding is modeled because it bites at $10-20 stakes (a 1-contract 50c order pays 2c = 4% of stake). kalshi_ev_maker dropped from a no-fee fantasy (12.50% on the test case) to 11.28%, with an adverse-selection caveat in code: resting bids fill disproportionately when the market moves against you, so maker EV is an upper bound. kalshi_n (contract count at current stake) now logged. (2) Polymarket: MLB/NFL/NBA/NHL game markets stay fee-free; NCAAB post-2026-02-18 markets carry a 0.0625 x P x (1-P) taker fee, applied automatically when CBB activates. (3) STALE-ANCHOR DISCARD: value recs anchored on a Pinnacle quote older than PIN_STALE_MIN (15) minutes are labeled on the board and NOT logged (phantom-edge risk A10, now mitigated by the bookmaker last_update field, captured as pin_age_min on every play). (4) NEWS-WINDOW RULE: any rec on a game whose probable pitcher changed THIS run sits out one cycle; the market is mid-move and our own anchor may be stale. Both suppressions counted in the runlog (stale_skips, news_skips) and printed. (5) Ledger notes from the report: Polymarket arb profits (~$40M documented) are heavily concentrated among fast actors, which supports the maker-patience approach over speed competition.

- **v13.2 (2026-07-04), audit batches 8-10, SCHEDULED PHASE COMPLETE (AUDIT_TODO F41/F42):** (1) Architecture (item 20): monthly snapshot rotation shipped (bounded live file, write-once monthly archives, nothing deleted, F13 closed) with model_version on every snapshot row; all keep-as-is verdicts documented with reasons (JSON over SQLite until >5k plays, sync pipeline over async, single-file over modules). (2) Community sweep (item 18): evidence-labeled, zero Reddit threads reachable and disclosed; worst-case/average devig added to the gated devig A/B, three-way additive note for future soccer, NFL open-vs-close academic caution recorded so MLB CLV expectations are not copied onto other sports. (3) AUDIT_DELIVERABLES.md created (item 19): ranked improvements with impact/difficulty/confidence/dependencies/validation, quick wins through major projects, tech-debt table, institutional roadmap, overlooked-ideas list; new permanent root file, rides in every zip. Remaining audit work is sample-gated only: item 11 at ~100 graded, item 10 at ~150, items 14/15 far future, plus optional research-mode 9b.

- **v13.1 (2026-07-04), Kalshi + Polymarket capture (AUDIT_TODO F39/F40; LOG-ONLY, MODEL_VERSION unchanged):** every ML play now logs both prediction-market quotes for its side: `kalshi_ticker/bid/ask` plus `kalshi_ev_taker` (fee-adjusted: cost = ask + 0.07*ask*(1-ask); the taker fee is ~4% of stake on a 40c dog and eats a 3% edge whole, so maker fills are the only sane Kalshi execution and `kalshi_ev_maker` at the bid is logged beside it) and `poly_mid/bid/ask` plus `poly_ev_mid` (no fee; overnight resting quotes are wide, the mid is the signal). `exec_best_venue`/`exec_best_ev` record which venue's fee-adjusted EV wins (Bovada vs BetOnline vs Kalshi vs Polymarket), LOG-ONLY; venue routing as a decision stays gated on the FUTURE roadmap. Live-verified on today's full slate: Kalshi 15/15 games matched via the KALSHI_MLB code map (codes are CWS and AZ, not CHW/ARI), Polymarket 15/15 via full-name titles after two non-obvious traps were fixed: event startDate is CREATION time (game markets open days early, so filter on the moneyline market's own gameStartTime), and derivative events ("First 5 Innings Winner") share the ' vs. ' title shape and must be excluded by the ' - ' suffix rule, or F5 prices masquerade as game moneylines. Kalshi series for NFL/NBA/NHL are wired but unverified until those seasons; college has no reliable PM game coverage mapping yet (deferred). Runlog gains pm_kalshi/pm_poly/pm_miss; a join alarm prints when team codes exist but no market matches.

- **v13 (2026-07-04), multi-sport activation (AUDIT_TODO F38):** NFL, NBA, NHL, CFB (ncaaf), CBB (ncaab) added to a new SPORTS registry with per-sport season-month windows; OFF-SEASON SPORTS ARE NEVER FETCHED (zero credits), and dashboard tabs generate from the registry with "back in <month>" states, so each sport lights up automatically when its window opens. All six Action Network slugs live-verified (identical payload shape; WNBA too, shipped disabled until F26 halves per-sport cost). New-sport plays use the _default sharp thresholds until each sport earns its own calibration; every play already carries 'sport', the EV-at-close segment table gains a per-sport row automatically when a second sport goes live, and the pre-registered protocol applies PER SPORT (F32/F33). MLB-only context (probables/weather/parks/scratches) is correctly gated to MLB plays. CREDIT REALITY, verified against official pricing: 5 overlapping sports in Oct-Nov at 2 full + 1 close/day projects ~2,100/month on regions billing or ~1,050 with F26, vs 500 free; the $30 20K plan makes it a non-issue (~5-21% utilization). A startup CREDIT WARNING now prints whenever the active-sport schedule projects over PLAN_CREDITS. MODEL_VERSION unchanged (MLB decision path untouched; new sports are new pre-registered streams under the same version, segmented by 'sport').

- **v12.3 (2026-07-04), Tier-NOW feature drop + source sweep (AUDIT_TODO F36/F37; all LOG-ONLY, MODEL_VERSION unchanged):** (1) Best-price capture: every play logs `bo_price` (BetOnline.ag, second executable offshore venue), `best_price`, `best_book` across vigged books at Bovada's exact point (same-point discipline enforced; exchanges excluded). This is the price-shopping dataset: worth roughly 0.5-1.5% EV per improved bet once execution switches. (2) Starter-scratch detector: probable-pitcher ids diffed per gamePk between runs via `ridgeseeker_probables.json` (same-date only); changes alarm on console, stamp new plays `probable_changed`, stamp PENDING plays `probable_changed_post_log`, and count into the runlog. (3) Weather: one multi-point open-meteo call (free, keyless) logs `om_temp_f`, `om_wind_kph`, `om_wind_dir_deg` at each park's start hour, plus `roof` and `park_rf_approx` from static tables (park factors are APPROXIMATE; swap in a Statcast export before any decision use; wind-out/in needs a park-azimuth table, deferred rather than guessed). (4) Source sweep: AN's free payload carries per-book pregame ML odds; now captured as `an_ml` on sharp entries, ML plays (our side), and snapshots; book-id mapping proven offline before any metric touches it. (5) `RS_MODE=observe`: full pipeline that NEVER logs plays, so the opener cron adds observations without silently changing entry timing. (6) New paste-in workflow: observe (13:30 UTC) and West-close (01:45 UTC) crons included but COMMENTED, plus `RS_BOOKMAKERS: '0'`; uncomment and flip to '1' only after the F26 curl passes (expanded schedule is ~330 credits/month WITH F26, ~660 and unaffordable without). RS_BOOKMAKERS now treats '', '0', 'false', 'no', 'off' as off. (7) bets.csv +12 columns, snapshots.csv +1, runlog +2 fields.

- **v12.2 (2026-07-03, same day), stance change + feature roadmap (docs only, no code):** Owner directive: the original audit's "do not add features" instruction is REMOVED. AUDIT_ORIGINAL.md (amended, dash-normalized) now lives in the repo root and every handback zip. FUTURE.md gains a tiered feature roadmap (5 Tier-NOW items led by second-book price capture and a starter-scratch detector, 6 gate-locked decision features, 4 seasonal/scale items) with impact/difficulty/confidence/dependencies/validation per entry. Batch 8 now implements Tier-NOW alongside the architecture review. Verbiage updated here and in AUDIT_TODO so no future chat re-derives the old stance.

- **v12.1 (2026-07-03, same day), audit batches 5-7 (items 8, 16, 12, 17, 13 + power analysis; AUDIT_TODO F30-F35):** (1) Monitoring shipped (item 13): feed-health strip that renders even with zero settled bets (last run, feed counts, alarms for empty odds feed, name-join misses, Bovada-absent games, MLB-context misses); per-run runlog file `ridgeseeker_runlog.json` (append-only, trimmed to 500 runs, exported as runlog.csv, joins betlog/snapshots in the NEVER-hand-back set); expected-vs-realized card; rolling-vs-cumulative clv_fair drift card; exploratory EV-at-close segments by market and run hour. Display and telemetry only, no MODEL_VERSION change. (2) BUG FIX (F35): batch 4 placed the internal-field pops before log_snapshots, which would have nulled every F23 snapshot field; fields now stripped after snapshots persist. (3) Pre-registered evaluation protocol written into FUTURE.md section 1: success and failure gates on mean clv_fair (50/150 measured), justified by power analysis (W/L proof of a 3.5% edge needs ~8,100 bets, ~11 seasons; clv_fair proof needs 15-100 measured closes). Decision logic frozen between N-gates. (4) Items 8/16 verdicts in the ledger: the honest market-efficiency frame, four pre-specified gated analyses over the v12 snapshot data, and microstructure conclusions including the execution-slippage plan for real money.

- **v12 (2026-07-03, same day), audit batch 4 (item 7 + F23/F26/F27/F29; see AUDIT_TODO ledger):** (1) A9 fixed at the source: exchange books (betfair_ex_eu, matchbook, matched by stable API key, never display title) are EXCLUDED from the vigged-consensus median that novig/power devig runs on; an exchange back price carries spread, not vig, and devigging it was a category error. This changes consensus composition and nb, hence MODEL_VERSION bump to v12-audit4. Pinnacle-anchored games (the vast majority) are numerically unaffected. (2) F27 log-only: exchange back/lay captured per side, plus `ex_mid` (midpoint of back/lay implied probs, a vig-free true-market fair) threaded through recs into every logged ML play; compared against the Pinnacle anchor at N before it is allowed near a decision. (3) F23: snapshots now persist per-book h2h prices, `h2h_disp` (implied-prob stdev across vigged books), and exchange quotes; enables bookmaker-disagreement features and per-bet EV confidence intervals later. (4) F29 log-only: one free keyless MLB Stats API call per full run stamps every logged play with `mlb_gamePk` (master join key to the entire MLB stats universe, makes most features retroactively reconstructable), probable pitcher names AND ids, venue, day/night, doubleheader flag, and weather when posted; join misses are counted and printed, never fatal; skipped in close mode. (5) F26 implemented but OFF: named-bookmaker fetch (10 curated books bill as 1 region, full run 6 to 3 credits, close run 2 to 1) behind `RS_BOOKMAKERS`; do not enable before the verification curl. (6) bets.csv +14 columns, snapshots.csv +2.

- **v11.2 (2026-07-03, same day), verification sweep + red-team (AUDIT_TODO F25):** (1) Clock guard at PLAY LOGGING: the pregame check trusted only the AN status feed (state in (None,'scheduled')), the exact bug class F6 fixed for closes. With AN down every game reads state None, so a delayed or manual run mid-slate would have logged LIVE in-play prices as pregame entries. Entry now also requires commence_time in the future by clock, mirroring the close guard; unparseable timestamps skip conservatively. No MODEL_VERSION bump (guard prevents contaminated logs, does not change EV math). (2) soft_fair_map now documents in code that it deliberately stays on proportional devig (F14, steam threshold tuned around it). (3) bets.csv exports void_reason. Every FIXED claim in the ledger (F1-F24) was verified against the code this pass; all present.

- **v11.1 (2026-07-03, same day), coverage check vs the original audit (AUDIT_TODO F22-F24):** (1) Stale-pending VOID pass in `grade_pending`: pending plays 72h past scheduled start grade as 'void', 0 units, excluded from record like pushes, surfaced in tracker bar, Results note, stats, and CSV. Nothing in the code handled postponed or cancelled games before this, despite earlier session notes claiming otherwise; a July rainout would have sat pending forever. MODEL_VERSION unchanged on purpose: version stamps segment DECISION changes, and this is settlement-only. (2) Risk-of-ruin numbers added to the ledger (F24): at 80u bankroll ($800 at $10/u), ruin within 500 bets is ~0.5% with a real +3.5% edge, ~2.1% at zero edge, ~6.2% at a NEGATIVE 3% edge; the ladder's step-down makes true risk lower still. Portfolio optimization declared N/A (single book, single sport, fixed units). (3) Documented gap F23 for batch 4: per-book prices are built in `analyze_game` every run and thrown away; snapshots persist consensus-level only, so bookmaker-dispersion features and per-bet EV confidence intervals have no data. Capture-or-lose.

- **v11 (2026-07-03), audit batch 2 (items 3, 4, 5; see AUDIT_TODO.md ledger F15-F21):** (1) CLV coverage hole closed: plays are seeded with close = entry at log time; previously day games and all 21:30-run plays never received a close and the primary metric silently covered a biased minority of plays. `close_obs` counts post-entry observations; only measured closes feed the stats, with coverage reported. (2) New headline metric `clv_fair`: entry price scored against the devigged (Pinnacle-anchored) closing fair probability, i.e. EV at close; resolves the "CLV vs Pinnacle" question in probability space instead of mixing vigged prices across books. (3) Close-capture run mode (`RS_MODE=close`, h2h only, 2 credits) plus a 22:45 UTC cron in the paste-in workflow; credit math now ~420/month. (4) Logging schema expanded with capture-or-lose fields: `run_ts`, raw Pinnacle prices (`pin_price`/`pin_opp`, lets any future devig method re-run on history), `nb`, raw `tickets`/`money`/`num_bets`/`steam_delta`, `units_reason`, `rec_type`, `close_fair`/`close_anchor`/`close_point`/`close_ts`/`close_obs`/`close_line_moved`; sharp-only plays now log their entry fair instead of None; CSVs export everything. (5) One-pending-play-per-game guard in `log_plays` (a rec flipping markets between runs logged correlated double exposure). (6) Risk: `MAX_DAILY_UNITS` exposure guidance surfaced in tracker bar + console (plays still logged); unit ladder kept over Kelly with the math documented; a variance-expectations card on the Results tab (simulation: even a real +3.5% edge shows ~15u median max drawdown and 8-loss streaks per 100 bets). (7) F10 name-join alarm: per-run printout when scheduled AN games fail to match Odds API games. (8) MODEL_VERSION v11-audit2.

- **v10 (2026-07-02), audit batch 1 (see AUDIT_TODO.md for the full findings ledger):** (1) SECURITY: removed the hardcoded Odds API key (env or gitignored odds_key.txt only); owner must rotate the exposed key. (2) Devig switched from proportional to the power method everywhere decisions are made; proportional still logged as `fair_mult` per play for later A/B on real results. (3) Score backfill: AN scoreboard fetched for the past 3 days each run so night games and missed days get graded; results keyed as lists of finals with start times, matched to plays via new `commence`/`event_id` fields. (4) Pushes now grade as 'push' (0 units) instead of pending forever. (5) `update_closes` hardened: clock-based pregame guard + event-id matching, protecting CLV from live-price contamination. (6) Dedupe key uses event_id, killing cross-day duplicate logging and doubleheader collisions. (7) bets.csv and snapshots.csv export the full field set (ev, fair, fair_mult, anchor, clv, close_price, commence, event_id, model_version). (8) MODEL_VERSION bumped to v10-audit1: pre-v10 plays used proportional devig, segment analyses accordingly. (9) Workflow replaced (paste-in `ridgeseeker.yml`, delete `edgefinder.yml`): the old one still ran `python edgefinder.py` and every scheduled run failed.

- **v9 (2026-07-02), renamed RidgeSeeker:** (1) Fair value is now anchored to devigged Pinnacle, fetched via `regions=us,eu` (the old "Pinnacle is paywalled" premise was wrong); consensus median remains the fallback and every play records its `anchor`. (2) Added closing line value: pending plays get `close_price` refreshed while pregame and `clv` computed at grading; Results tab shows avg CLV and beat-the-close rate, plus a new "By stated EV" table. (3) Logged plays now carry `model_version`, `ev`, `fair`, `anchor`. (4) Doubleheader finals are detected as ambiguous and skipped rather than coin-flip graded. (5) Dashboard text no longer claims Kalshi/Polymarket sources that were never implemented (honesty fix). (6) File renames with auto-migration: `ridgeseeker.py`, `ridgeseeker_betlog.json`, `ridgeseeker_snapshots.json`, `ridgeseeker_history/`; old EdgeFinder files are adopted on first run, history preserved. (7) Workflow (paste-in): name RidgeSeeker, 2 scheduled runs/day (15:00 and 21:30 UTC), concurrency guard, pull-rebase before push, snapshots file now committed. Credit math: 6/run x 2/day = ~360 of ~500 free monthly.

- **v8 (2026-07-01):** Removed the dead standalone `build_stats_page` function and all `stats.html` writes. There is now exactly one stats system: the integrated Results tab inside `index.html`. Replaced the remaining em-dash placeholders in table cells with plain hyphens (file now has zero em dashes). No behavior change to grades, sizing, fetching, or tracking.
- **v7 (2026-07-01):** MLB-only focus; grades recalibrated to the MLB percentile distribution (B is the average, A/S rare); unit sizing with +250 longshot cap; automatic results tracker with level-up gates; per-run snapshot logging for edge-over-time; Results view folded into the main dashboard via a Board/Results pill toggle; CSV exports; parlay removed; 3x/day GitHub Actions schedule.
- (Earlier history predates this file; reconstruct from git log if needed.)

---

*When you finish a change: bump the doc version, add a changelog entry saying what changed and why, update any section above that no longer matches the code, and remind the owner to commit ridgeseeker.py, HANDOFF.md, and FUTURE.md together.*
